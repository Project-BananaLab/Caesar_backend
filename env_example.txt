# Caesar MCP 프로젝트 환경 변수 예시
# 실제 사용 시 .env 파일로 복사하여 값 설정

# 데이터베이스 설정
DATABASE_TYPE=sqlite
DATABASE_URL=sqlite:///./caesar.db
# PostgreSQL 사용 시:
# DATABASE_TYPE=postgresql
# DATABASE_URL=postgresql://username:password@localhost/caesar_db

# Google APIs
GOOGLE_DRIVE_CREDENTIALS=./credentials/google_drive_credentials.json
GOOGLE_CALENDAR_CREDENTIALS=./credentials/google_calendar_credentials.json

# Notion
NOTION_TOKEN=your_notion_integration_token

# Slack
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# RAG 설정
VECTOR_DB_PATH=./vector_db
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# API 서버 설정
API_HOST=0.0.0.0
API_PORT=8000

# 로그 레벨
LOG_LEVEL=INFO

